import { defineMessages } from 'react-intl'


const scope = 'components-ceacle.NavBar'

export default defineMessages({
  navSignIn: {
    id: `${scope}.navSignIn`,
    defaultMessage: 'Sign in',
  },
})
