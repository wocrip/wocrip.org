import { defineMessages } from 'react-intl'


const scope = 'components-shared.ProviderLogin'

export default defineMessages({
  validProviderAuthFailed: {
    id: `${scope}.validProviderAuthFailed`,
    defaultMessage: '{provider} authorization failed',
  },
})
