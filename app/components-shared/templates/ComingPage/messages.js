import { defineMessages } from 'react-intl'


const scope = 'components-shared.ComingSoon'

export default defineMessages({
  backHome: {
    id: `${scope}.backHome`,
    defaultMessage: 'Back Home',
  },
})
