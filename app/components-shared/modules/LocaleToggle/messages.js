import { defineMessages } from 'react-intl'

const scope = 'components-shared.modules.LocaleToggle'

export default defineMessages({
  en: {
    id: `${scope}.en`,
    defaultMessage: 'English',
  },
  fr: {
    id: `${scope}.fr`,
    defaultMessage: 'Français',
  },
})
